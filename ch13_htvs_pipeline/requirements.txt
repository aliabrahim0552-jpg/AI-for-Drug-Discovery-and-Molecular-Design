# Tested with rdkit 2026.03.5, meeko 0.7.1, numpy 2.5.2, scipy 1.18.0,
# requests 2.34.2, openmm 8.6.0, openmm-ml (openmmml) 1.7, torchani
# 2.8.4, and OpenBabel 3.1.0 on Python 3.12. vina>=1.2.5 on platforms
# with a prebuilt wheel (manylinux/musllinux/macOS); this chapter's own
# authoring environment (Windows, no `vina` wheel -- see Chapter 11's
# feasibility note) used the official standalone Vina CLI binary via
# the VINA_EXECUTABLE environment variable instead -- see README.md.
rdkit>=2023.9
meeko>=0.6
numpy>=1.24
scipy>=1.10
requests>=2.28
openmm>=8.5
openmmml>=1.4
torchani>=2.2
vina>=1.2.5; sys_platform != "win32"
