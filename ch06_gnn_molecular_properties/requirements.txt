# Tested with torch 2.13.0 (CPU build), torch_geometric 2.8.0.post1,
# rdkit 2026.3.5, and scikit-learn 1.9.0 on Python 3.12. Install torch
# from the CPU wheel index first if you don't have a CUDA GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torch_geometric>=2.5
rdkit>=2023.9.1
scikit-learn>=1.3
numpy>=1.24
